server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          basedir: config-repo/tmp
          uri: https://github.com/RockeyCui/config/
          searchPaths: backend,usercenter
          username:
          password:
        encrypt:
          enabled: false